<section class="tab-panel talents">
	<h2>{{ localize "aegean.system.GiftsTalents" }}</h2>
	{{!-- #each talents}}
		<div class="talent">
			<h3>{{ talent.title }}</h3>
			<p>{{ talent.description }}</p>
		</div>
	{{/each --}}
</section>
